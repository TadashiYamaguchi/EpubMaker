<Window x:Class="ProgressWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		mc:Ignorable="d"
		Title="変換中" Height="130" Width="360" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
	<StackPanel Margin="20">
		<TextBlock Text="{Binding Message}" Margin="0,0,0,10"/>
		<ProgressBar Minimum="0" Maximum="{Binding Total}" Value="{Binding Current}" Height="20"/>
		<TextBlock Margin="0,5,0,0" HorizontalAlignment="Right">
			<Run Text="{Binding Current, Mode=OneWay}"/> / <Run Text="{Binding Total, Mode=OneWay}"/>
		</TextBlock>
	</StackPanel>
</Window>
